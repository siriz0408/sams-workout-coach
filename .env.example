# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Note: API keys for AI services (OpenAI, Anthropic) should be set
# in the Supabase dashboard as Edge Function secrets, NOT here.
# These are for client-safe values only (prefixed with EXPO_PUBLIC_)
